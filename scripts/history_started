2026-09-02

The first day in history-v1.json. See load_history() in build_index.py: while this
file exists, a 404 on the published history is fatal instead of being read as a
first run. Delete it only to start the download history over on purpose.
